#!/bin/bash

USER_ID=${1:?No user ID specified on command line}
useradd --shell /bin/bash -u ${USER_ID} -o -c "" -m user${USER_ID}
export HOME=$(bash -c "echo ~user${USER_ID}")
exec /usr/sbin/gosu user${USER_ID} "${@:2}"


